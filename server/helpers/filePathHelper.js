//Only needed if using ECMAScript import/export syntax on backend,
// doesn't work with webpack at this moment

// const { fileURLToPath } = require("url");

// export function getDirectoryName(moduleUrl) {
//   const filename = fileURLToPath(moduleUrl);
//   return Promise.resolve(filename);
// }
